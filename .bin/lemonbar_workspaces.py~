import json
import sys

input_str = sys.stdin.read()
input_converted = json.loads(input_str)

for workspace in input_converted:
    if(workspace['focused']==True):
        print "%{R}"+str(workspace['num'])+"%{U#FFFFFF}%{R}",
    else: 
        print str(workspace['num']),
